unsafe fn raw(value: Int): Int {
    value + 1
}
async fn delayed(): Int {
    1
}
async fn valid() {
    let direct = unsafe {
        raw(40)
    }
    let later = await delayed()
    assert(direct + later == 42)
}
