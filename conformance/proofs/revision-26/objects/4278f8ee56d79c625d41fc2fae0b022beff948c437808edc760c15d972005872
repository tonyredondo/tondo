fn consume[T: Discard](values: ...T) {
    _ = values
}

fn make(value: Int): impl CallOnce[fn(): Int] + Discard {
    () {
        value
    }
}

fn main() {
    let operation = make(42)
    consume(operation)
    _ = operation
}
