import std.console
import std.process

fn cancelProcess(): !process.ProcessError suspends {
    scope {
        let pending = spawn process.shell("exec sleep 30").output()
        return
    }
}

fn main(): !process.ProcessError suspends {
    cancelProcess()?
    console.print("process-cancelled\n")
}
