import std.console
import std.process

fn cancelProcess(): !process.ProcessError {
    scope {
        let pending = spawn process.shell("exec sleep 30").output()
        return
    }
}

fn main(): !process.ProcessError {
    cancelProcess()?
    console.print("process-cancelled\n")
}
