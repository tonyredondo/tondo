unsafe fn raw(value: Int): Int {
    value + 1
}
fn delayed(): Int suspends {
    1
}
fn valid() {
    let direct = unsafe {
        raw(40)
    }
    let later = delayed()
    assert(direct + later == 42)
}
