import std.console

fn main() {
    var original = [[1], [2]]
    var copied = original
    copied[0][0] = 9
    assert(original[0][0] == 1)
    assert(copied[0][0] == 9)
    console.print("panic\n")
    _ = copied[2]
}
