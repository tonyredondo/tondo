fn observe(value: ref Int): Int suspends {
    value
}

fn invalid() suspends {
    scope {
        var value = 1
        let pending = spawn observe(ref value)
        value = 2
        let observed = await pending
        assert(observed == 1)
    }
}
