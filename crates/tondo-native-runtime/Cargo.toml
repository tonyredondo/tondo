[package]
name = "tondo-native-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
tondo-stdlib = { path = "../tondo-stdlib" }

[lib]
crate-type = ["rlib", "staticlib"]

[lints]
workspace = true
