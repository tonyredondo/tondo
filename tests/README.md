# Compiler fixtures

The fixture protocol is defined in `docs/contracts/testing.md`. Integration
tests discover cases from the four directories below in sorted logical-path
order.

No fixture is enabled merely by existing: its directory contract and sidecars
determine the expected behavior.
