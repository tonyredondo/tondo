import std.console

fn tick() suspends {}

fn cleanup() suspends {
    _ = console.print("cleanup\n")
}

fn child() {
    defer cleanup()
    for {
        tick()
    }
}

fn main() {
    scope {
        let pending = spawn child()
        return
    }
    _ = console.print("parent\n")
}
