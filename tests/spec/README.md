# Specification fixtures

This directory will contain cases extracted from normative Markdown fences.
Every generated case must retain its spec revision and fence identity in an
adjacent manifest. Every case has a `.codes` sidecar, including an empty one
when no diagnostic is expected. Extraction begins with M1.
