# Compile-fail fixtures

A `.to` case must be rejected. Its `.codes` sidecar lists the exact primary
diagnostic codes in order. Human or JSON snapshots are added when their complete
shape is part of the regression.
