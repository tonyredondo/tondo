{
  "format": "tondo-stdlib-owner-contract/1",
  "owner": "std.time",
  "edition": "0.1",
  "phase": "STD-0.1A",
  "status": "draft-contract",
  "contract": "docs/contracts/stdlib-time.md",
  "layer": "A0",
  "kind": "capability-gated",
  "target": "tondo-vm-hosted",
  "profile": "time",
  "api": "tondo-std-time-0.1/1",
  "source": {
    "path": "crates/tondo-compiler/src/process_host.rs",
    "sha256": "sha256:ac51c6559f1dcb30673e7acf1e56d654e7e727632c7f55c77d1b634f8d2f1d92"
  },
  "capabilities": {
    "required": ["clock"],
    "forbidden": [
      "filesystem",
      "environment",
      "process",
      "entropy",
      "network",
      "threads",
      "dynamic-loading",
      "runtime-value-reflection",
      "ambient-host"
    ]
  },
  "invariants": [
    "monotonic-within-domain",
    "opaque-provider-domain-identity",
    "checked-duration-arithmetic",
    "affine-one-shot-timer",
    "atomic-resource-reservation",
    "explicit-virtual-advance",
    "real-virtual-semantic-equivalence",
    "capability-gated-clock",
    "no-wall-clock-calendar-leak"
  ],
  "limits": [
    {
      "id": "duration_nanoseconds",
      "unit": "nanoseconds",
      "source": "Int domain"
    },
    {
      "id": "instant_value",
      "unit": "nanoseconds",
      "source": "Int domain"
    },
    {
      "id": "active_time_resources",
      "unit": "timers-and-jobs",
      "source": "BootstrapHost.max_time_resources"
    },
    {
      "id": "virtual_advance",
      "unit": "nanoseconds",
      "source": "Int domain"
    },
    {
      "id": "clock_resolution",
      "unit": "nanoseconds",
      "source": "positive Duration"
    }
  ],
  "test_matrix": [
    {
      "id": "model-and-identity",
      "required": true,
      "observables": ["duration", "instant", "timer", "provider-domain", "ownership-capabilities"]
    },
    {
      "id": "real-provider",
      "required": true,
      "observables": ["monotonic-now", "positive-resolution", "deadline", "real-sleep"]
    },
    {
      "id": "virtual-provider",
      "required": true,
      "observables": ["zero-origin", "exact-resolution", "explicit-advance", "no-wall-clock-wait"]
    },
    {
      "id": "limits-and-errors",
      "required": true,
      "observables": ["duration-overflow", "negative-delay", "deadline-overflow", "domain-mismatch", "resource-limit"]
    },
    {
      "id": "timers-and-cancellation",
      "required": true,
      "observables": ["one-shot", "equal-deadline-order", "wait-consumption", "cancel-cleanup", "atomic-release"]
    },
    {
      "id": "capability-and-conformance",
      "required": true,
      "observables": ["clock-capability", "real-virtual-equivalence", "runtime-fixture", "source-interface-evidence"]
    }
  ],
  "corpora": [
    {
      "id": "time-arithmetic-boundaries",
      "source": "owner-generated",
      "required": true,
      "focus": ["zero", "negative", "int-min", "int-max", "overflow", "unit-conversion"]
    },
    {
      "id": "provider-equivalence",
      "source": "owner-generated",
      "required": true,
      "focus": ["real", "virtual", "resolution", "now", "deadline", "zero-delay"]
    },
    {
      "id": "timer-lifecycle",
      "source": "owner-generated",
      "required": true,
      "focus": ["wait", "cancel", "tie-order", "foreign-domain", "resource-release"]
    }
  ],
  "promotion": {
    "gates": [
      {"id": "design", "required": true},
      {"id": "implementation", "required": true},
      {"id": "conformance", "required": true},
      {"id": "performance", "required": true},
      {"id": "promote", "required": true}
    ],
    "next_coordination": "STD-A-ENV-EVIDENCE-001"
  }
}
