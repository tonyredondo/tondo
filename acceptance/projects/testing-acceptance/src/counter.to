import std.testing
import std.time

fn answer(): Int {
    42
}

pub fn answerAfterBackoff(delay: time.Duration): Int ! time.ClockError suspends {
    time.sleep(delay)?
    answer()
}

test production_api {
    testing.log("unit production API")
    testing.tags(["kind": "unit"])
    assert(answer() == 42)
}
